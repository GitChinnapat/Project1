# 📦 Moving System - คู่มือการใช้งาน

## 🎯 บรรยายระบบ

ระบบย้ายของ (Moving System) เป็นฟีเจอร์ที่ให้ผู้ใช้สามารถ:
1. **ส่งคำขอย้ายของ** ด้วยรายละเอียดสถานที่ ประเภทงาน และรายละเอียด
2. **ดูประวัติการขอย้ายของ** ที่ได้ส่งมาแล้ว
3. **ลบคำขอย้ายของ** ที่ไม่ต้องการแล้ว

---

## 📁 ไฟล์ที่สร้างใหม่

### Backend
- **Server/controllers/movingController.js** - Controller สำหรับจัดการ moving requests
- **Server/routes/moving.js** - Routes API endpoints สำหรับ moving
- **Server/moving.sql** - SQL script สำหรับสร้างตาราง moving

### Frontend
- **Client/src/page/Moving.jsx** - Form สำหรับส่งคำขอย้ายของ
- **Client/src/page/MovingList.jsx** - Page สำหรับแสดงรายการคำขอย้ายของ

### Modified Files
- **Server/config/initialize.js** - เพิ่มโค้ดสร้างตาราง moving
- **Server/index.js** - เพิ่ม moving routes
- **Client/src/services/api.js** - เพิ่ม movingAPI object
- **Client/src/routes/GuestRoutes.jsx** - เพิ่ม MovingList route

---

## 🗄️ Database Schema

```sql
CREATE TABLE moving (
  move_id INT NOT NULL AUTO_INCREMENT,        -- รหัสการขอย้าย (auto-increment)
  user_id INT NOT NULL,                       -- รหัสผู้ใช้
  user_name VARCHAR(255) NOT NULL,            -- ชื่อผู้ขอย้าย
  location VARCHAR(255) NOT NULL,             -- สถานที่ย้ายไป
  type_work VARCHAR(100) NOT NULL,            -- ประเภทงาน (เช่น ย้ายออฟฟิศ, ย้ายสินค้า)
  detail TEXT NOT NULL,                       -- รายละเอียด
  img LONGTEXT,                               -- รูปภาพ (ชื่อไฟล์)
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, -- วันที่สร้าง
  PRIMARY KEY (move_id)
);
```

---

## 🔌 API Endpoints

### 1. Create Moving Request (POST)
```
POST /api/moving
Content-Type: application/json

Request Body:
{
  "user_id": 1,
  "user_name": "John Doe",
  "location": "Room 101",
  "type_work": "Moving",
  "detail": "Need to move office equipment",
  "img": null
}

Response (201):
{
  "success": true,
  "message": "ส่งคำขอย้ายของสำเร็จแล้ว",
  "data": {
    "id": 1,
    "user_id": 1,
    "user_name": "John Doe",
    ...
  }
}
```

### 2. Get All Moving Requests (GET)
```
GET /api/moving

Response (200):
{
  "success": true,
  "message": "ข้อมูลการย้ายของทั้งหมด",
  "data": [
    {
      "move_id": 1,
      "user_id": 1,
      "user_name": "John Doe",
      "location": "Room 101",
      "type_work": "Moving",
      "detail": "...",
      "created_at": "2024-12-24T10:30:00.000Z"
    }
  ]
}
```

### 3. Get Moving by ID (GET)
```
GET /api/moving/:id

Response (200):
{
  "success": true,
  "data": { ... }
}
```

### 4. Update Moving (PUT)
```
PUT /api/moving/:id
Content-Type: application/json

Request Body:
{
  "location": "Room 102",
  "type_work": "Moving",
  "detail": "Updated detail"
}

Response (200):
{
  "success": true,
  "message": "อัปเดตคำขอย้ายของสำเร็จแล้ว"
}
```

### 5. Delete Moving (DELETE)
```
DELETE /api/moving/:id

Response (200):
{
  "success": true,
  "message": "ลบคำขอย้ายของสำเร็จแล้ว"
}
```

---

## 🎨 Frontend Pages

### Moving.jsx
- **Route**: `/RMUTI/Moving`
- **คำอธิบาย**: Form สำหรับส่งคำขอย้ายของ
- **Fields**:
  - สถานที่ (location)
  - ประเภทงาน (jobType)
  - รายละเอียด (details)
  - รูปภาพ (images)
- **ฟีเจอร์**:
  - ดึง user data จาก localStorage
  - Validation ทั้งฝั่ง Client และ Server
  - Success/Error message notification
  - Loading state ขณะส่งข้อมูล

### MovingList.jsx
- **Route**: `/RMUTI/MovingList`
- **คำอธิบาย**: Page แสดงรายการคำขอย้ายของทั้งหมด
- **ฟีเจอร์**:
  - โหลดข้อมูลอัตโนมัติ
  - ปุ่ม Refresh เพื่อโหลดข้อมูลใหม่
  - ปุ่ม Delete เพื่อลบแต่ละรายการ
  - แสดงวันที่และเวลา (Thai format)
  - Loading indicator
  - Error handling

---

## 📝 Code Flow

### ส่งคำขอย้ายของ
```
User fills form
    ↓
Click ตกลง
    ↓
Validation (Client-side)
    ↓
Get user data from localStorage
    ↓
POST /api/moving
    ↓
Validation (Server-side)
    ↓
Insert to database
    ↓
Show success message
    ↓
Reset form
```

### ดูรายการคำขอ
```
Load MovingList page
    ↓
GET /api/moving
    ↓
Fetch all moving requests
    ↓
Display in list format
    ↓
User can delete or refresh
```

---

## 🧪 Testing

### 1. ทดสอบด้วย Browser
```
1. เข้า http://localhost:5173/RMUTI/Moving
2. ล็อกอินก่อน
3. กรอกฟอร์ม
4. คลิก "ตกลง"
5. ตรวจสอบข้อความ success
6. ไปที่ /RMUTI/MovingList เพื่อดูรายการ
```

### 2. ทดสอบด้วย cURL
```bash
# Create moving
curl -X POST http://localhost:5000/api/moving \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 1,
    "user_name": "John Doe",
    "location": "Room 101",
    "type_work": "Moving",
    "detail": "Test moving request",
    "img": null
  }'

# Get all moving
curl http://localhost:5000/api/moving

# Delete moving
curl -X DELETE http://localhost:5000/api/moving/1
```

### 3. ทดสอบด้วย MySQL
```sql
USE servicereq_db;

-- ตรวจสอบตาราง
DESCRIBE moving;

-- ดูข้อมูลทั้งหมด
SELECT * FROM moving;

-- ดูตามวันที่
SELECT * FROM moving WHERE DATE(created_at) = CURDATE();
```

---

## 🔍 Troubleshooting

### ❌ Error: "Table 'moving' doesn't exist"
**วิธีแก้:**
1. รัน SQL: `CREATE TABLE moving (...)`
2. หรือรูปแบบ: ลบและเปิด Server ใหม่ (auto-create)

### ❌ Error 400: "กรุณากรอกข้อมูลที่จำเป็นให้ครบ"
**สาเหตุ:**
- ฟอร์มไม่ได้กรอกข้อมูลให้ครบ
**วิธีแก้:**
- ตรวจสอบว่ากรอกทุก field

### ❌ Error 500: "เกิดข้อผิดพลาดในการส่งคำขอย้ายของ"
**สาเหตุ:**
- Database error หรือ query error
**วิธีแก้:**
- ตรวจสอบ Server logs
- ตรวจสอบตาราง moving มีอยู่หรือไม่

### ❌ Error: "กรุณาเข้าสู่ระบบก่อนส่งคำขอย้ายของ"
**สาเหตุ:**
- ไม่มีข้อมูล user ใน localStorage
**วิธีแก้:**
- ล็อกอินใหม่

---

## 📊 System Integration

Moving system ทำงานร่วมกับ:
- **Auth System** - ดึง user data จาก localStorage
- **Database** - บันทึก/ดึงข้อมูลจาก moving table
- **API** - ส่ง/รับข้อมูลผ่าน HTTP requests

---

## 🚀 การใช้งาน

### สำหรับผู้ใช้
1. เข้าสู่ระบบ
2. ไปที่ "ขนย้าย / จัดสถานที่"
3. กรอกรายละเอียด
4. คลิก "ตกลง"
5. ดูประวัติที่ "รายการย้ายของ"

### สำหรับ Admin
1. เข้า MySQL
2. ดูข้อมูลคำขอ: `SELECT * FROM moving`
3. ลบคำขอหากจำเป็น: `DELETE FROM moving WHERE move_id = X`

---

## ✅ Validation Rules

### Client-side
- สถานที่ต้องไม่ว่าง
- ประเภทงานต้องเลือก
- รายละเอียดต้องไม่ว่าง
- User ต้องล็อกอินแล้ว

### Server-side
- user_id ต้องเป็นตัวเลข > 0
- user_name ต้องเป็นสตริงและไม่ว่าง
- location, type_work, detail ต้องไม่ว่าง
- ตรวจสอบทั้งหมดก่อนบันทึก

---

## 📌 Notes

- ระบบจะบันทึก user_id และ user_name พร้อมคำขอ
- สามารถลบคำขอได้ในหน้า MovingList
- วันที่จะแสดงเป็น Thai format อัตโนมัติ
- รูปภาพจะบันทึกเป็นชื่อไฟล์เท่านั้น (ไม่เก็บไฟล์จริง)

---

**เวอร์ชัน:** 1.0.0  
**วันสร้าง:** 2024-12-24  
**สถานะ:** ✅ Working
