# 🚀 Quick Start Guide - ระบบแจ้งซ่อม

## ⚡ เริ่มต้นใช้งาน (5 นาที)

### 1️⃣ MySQL Setup
```bash
# ตรวจสอบว่า MySQL Server ทำงาน
net start MySQL80

# เปิด MySQL Workbench หรือ MySQL CLI แล้วรัน:
CREATE DATABASE IF NOT EXISTS servicereq_db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2️⃣ Backend - Terminal 1
```bash
cd e:\Project_Final\Server
npm install
npm start
```

**รอจนกว่าจะเห็น:**
```
✅ Database connected successfully
✅ Repair table created or already exists
🚀 Server running on http://localhost:5000
```

### 3️⃣ Frontend - Terminal 2
```bash
cd e:\Project_Final\Client
npm install
npm run dev
```

**รอจนกว่าจะเห็น:**
```
VITE v... ready in ... ms
➜  Local:   http://localhost:5173/
```

### 4️⃣ ใช้งาน
- เปิดเบราว์เซอร์ไปยัง `http://localhost:5173/`
- คลิก "เข้าสู่ระบบ" 
- ไปหน้า `/repair` เพื่อแจ้งซ่อม
- กรอกข้อมูล → คลิก "ตกลง"
- ข้อมูลจะบันทึกในฐานข้อมูล ✅

---

## 📋 Checklist ต้องทำ

- [ ] MySQL Server ทำงาน
- [ ] ฐานข้อมูล `servicereq_db` สร้างแล้ว
- [ ] Backend เริ่มต้นได้ (port 5000)
- [ ] Frontend เริ่มต้นได้ (port 5173)
- [ ] ทดสอบฟอร์มแจ้งซ่อม
- [ ] ตรวจสอบข้อมูลในฐานข้อมูล

---

## 🔍 ตรวจสอบข้อมูล

ดูข้อมูลแจ้งซ่อมที่บันทึก:
```sql
SELECT * FROM repair;
```

---

## ❓ FAQ

**Q: Error "Cannot connect to database"?**
A: ตรวจสอบให้แน่ใจว่า MySQL Server ทำงาน

**Q: Error "ERR_NAME_NOT_RESOLVED"?**
A: ปลายทาง API ยังไม่ได้เริ่มต้น ดู Terminal 1

**Q: ข้อมูลไม่บันทึก?**
A: ตรวจสอบ browser console ว่ามี error อะไร

---

## 📞 Support

ดู `TROUBLESHOOTING_REPAIR_SYSTEM.md` สำหรับความช่วยเหลือเพิ่มเติม
