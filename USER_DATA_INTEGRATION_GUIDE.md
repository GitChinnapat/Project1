# 📋 คู่มือ - ดึงข้อมูลผู้ใช้มาลงในฐานข้อมูล

## 🎯 ที่ทำแล้ว

ระบบแจ้งซ่อมตอนนี้ได้เพิ่มฟีเจอร์ดึงข้อมูลผู้ใช้มาเก็บ:

### ✅ ฟีเจอร์ใหม่:
1. **เก็บรหัสผู้ใช้** (`user_id`) - เชื่อมโยงกับตาราง `users`
2. **เก็บชื่อผู้แจ้ง** (`user_name`) - ชื่อของผู้แจ้งซ่อม
3. **แสดงรายชื่อผู้แจ้ง** - ในแต่ละรายการแจ้งซ่อม

---

## 🗄️ โครงสร้างฐานข้อมูล

### ตาราง repair ตอนนี้มี:

```sql
CREATE TABLE repair (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,            -- ← ใหม่ รหัสผู้ใช้
  user_name VARCHAR(255) NOT NULL, -- ← ใหม่ ชื่อผู้แจ้ง
  location VARCHAR(255) NOT NULL,
  type_work VARCHAR(100) NOT NULL,
  detail TEXT NOT NULL,
  img LONGTEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

---

## 🔄 วิธีการทำงาน

### 1. ผู้ใช้เข้าสู่ระบบ (Login)
```javascript
// ข้อมูลถูกเก็บใน localStorage
localStorage.setItem('user', JSON.stringify({
  id: 1,
  name: "สมชาย ใจดี",
  email: "somchai@example.com",
  ...
}));
```

### 2. ผู้ใช้แจ้งซ่อม
```javascript
// ระบบอ่านข้อมูลจาก localStorage
const userData = JSON.parse(localStorage.getItem('user'));

// ส่งไปกับคำร้องขออื่นๆ
const repairData = {
  user_id: userData.id,                    // ← ID ผู้ใช้
  user_name: userData.name,                // ← ชื่อผู้ใช้
  location: "อาคาร A",
  type_work: "electric",
  detail: "ไฟเสีย",
  img: null
};
```

### 3. API รับข้อมูล
```javascript
// POST /api/repair
// request body ประกอบด้วย user_id และ user_name
```

### 4. บันทึกลงฐานข้อมูล
```sql
INSERT INTO repair (user_id, user_name, location, type_work, detail, img)
VALUES (1, "สมชาย ใจดี", "อาคาร A", "electric", "ไฟเสีย", null);
```

---

## 📄 ไฟล์ที่แก้ไข

| ไฟล์ | อะไรเปลี่ยน |
|-----|-----------|
| `Server/database.sql` | เพิ่ม `user_id`, `user_name` |
| `Server/config/initialize.js` | เพิ่ม column ใหม่ในตาราง |
| `Server/controllers/repairController.js` | รับ `user_id`, `user_name` ในคำร้อง |
| `Client/src/page/Repair.jsx` | ดึงข้อมูลผู้ใช้จาก localStorage |
| `Client/src/page/RepairList.jsx` | ✨ ใหม่ - แสดงรายการแจ้งซ่อมพร้อมชื่อผู้แจ้ง |

---

## 🚀 ใช้งาน

### Step 1: อัปเดตฐานข้อมูล
รัน SQL script ใหม่:
```bash
# อ่านจาก Server/database.sql
```

### Step 2: Restart Server
```bash
cd Server
npm start
```

### Step 3: เข้าสู่ระบบและแจ้งซ่อม
1. เข้าสู่ระบบ (Login) - ข้อมูลผู้ใช้จะเก็บใน localStorage
2. ไปหน้า `/repair`
3. กรอกข้อมูลและส่ง
4. ข้อมูลจะบันทึกพร้อมชื่อผู้แจ้ง ✅

### Step 4: ดูรายการแจ้งซ่อม
ไปที่หน้า `/repair-list` เพื่อดูรายการแจ้งซ่อมทั้งหมดพร้อมชื่อผู้แจ้ง

---

## 📊 ตัวอย่างข้อมูล

```sql
SELECT * FROM repair;

-- ผลลัพธ์:
-- id=1, user_id=1, user_name="สมชาย ใจดี", location="อาคาร A ชั้น 2", ...
-- id=2, user_id=2, user_name="สมหญิง สวย", location="อาคาร B ชั้น 3", ...
```

---

## ⚙️ การตั้งค่า localStorage

ระบบอ่านข้อมูลผู้ใช้จาก:
```javascript
const userData = JSON.parse(localStorage.getItem('user'));

// ต้องมี properties:
// - id (รหัสผู้ใช้)
// - name หรือ email (ชื่อผู้ใช้)
```

หากไม่มี localStorage ระบบจะแสดง error:
```
"กรุณาเข้าสู่ระบบก่อนแจ้งซ่อม"
```

---

## 🎓 SQL Query ตัวอย่าง

### ดูการแจ้งซ่อมทั้งหมดของผู้ใช้รายหนึ่ง
```sql
SELECT * FROM repair
WHERE user_id = 1;
```

### ดูการแจ้งซ่อมพร้อมชื่อผู้ใช้
```sql
SELECT r.*, u.name as user_name
FROM repair r
JOIN users u ON r.user_id = u.id
ORDER BY r.created_at DESC;
```

### นับการแจ้งซ่อมของแต่ละผู้ใช้
```sql
SELECT user_id, user_name, COUNT(*) as total
FROM repair
GROUP BY user_id, user_name;
```

---

## ✅ Checklist

- [ ] อัพเดทฐานข้อมูล (เพิ่ม user_id, user_name)
- [ ] Restart backend server
- [ ] เข้าสู่ระบบ (Login)
- [ ] ไปหน้า /repair แจ้งซ่อม
- [ ] ตรวจสอบฐานข้อมูลว่ามี user_name
- [ ] ดูรายการแจ้งซ่อม (ที่ /repair-list)

---

หากมีปัญหา ให้ตรวจสอบ:
1. ✅ localStorage มี user data หรือไม่ (เข้าสู่ระบบสำเร็จหรือไม่)
2. ✅ ฐานข้อมูล repair table มี user_id, user_name column หรือไม่
3. ✅ Backend ทำงานสำเร็จหรือไม่ (ดู console)
